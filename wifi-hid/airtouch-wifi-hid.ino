// AirTouch WiFi HID — ESP32-S2/S3 USB HID mouse via WiFi PeerJS
// Stub: fill WiFi + WebSocket + TinyUSB HID
// - Connects WiFi
// - Connects to wss://0.peerjs.com or your relay ?room=ROOM
// - On {type:"move",dx,dy} -> USB HID report
#include <WiFi.h>
// #include "USBHIDMouse.h" // TinyUSB
// USBHIDMouse Mouse;
void setup(){ /* WiFi.begin(...); Mouse.begin(); */ }
void loop(){ /* if(ws.available()){ parse dx,dy; Mouse.move(dx,dy); } */ }
